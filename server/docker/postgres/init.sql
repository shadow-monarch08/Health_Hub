-- Create database (handled by env var usually, but strictly following instructions)
-- CREATE DATABASE healthhub;
-- CREATE USER healthhub WITH ENCRYPTED PASSWORD 'healthhub_pass';
-- GRANT ALL PRIVILEGES ON DATABASE healthhub TO healthhub;

-- Basic setup if needed
SELECT 1;
